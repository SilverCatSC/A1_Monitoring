# Состояние релиза A1 Search Monitor

Срез: 5 сентября 2026 года. Текущая версия: stage `0.4.1`.

## Проверено

### Данные

- Google Sheets CSV-export доступен и импортируется без marketplace API.
- Реальный снимок: 130 строк, 106 валидных, 24 диагностических.
- 34 автомобиля активны; распознано 78 Auto.ru и 76 Avito ссылок.
- Живые заголовки подтверждены: `Анализ_Размещения!A:M` содержит объединённое
  поле «Марка, модель» и целевые J:M, а исходные ссылки находятся в AD:AE листа
  `Размещение_автомобилей_Копия`.
- Объединённое имя разделяется только по известному справочнику марок; неизвестное
  значение сохраняется целиком и не угадывается по первому пробелу.
- Перестановка/неизвестные столбцы, алиасы, дубли и несколько VIN покрыты тестами.
- Резкое падение валидного снимка и критичный schema drift блокируют замену реестра.
- Управляемые фильтры `all_active` синхронизируются после импорта.
- Инструкция владельца перенесена в каталог версии
  `a1-monitoring-instruction-2026-09-04-v1`: 10 Auto.ru и 6 уникальных Avito
  сценариев, 38 назначений на текущем снимке.
- Все 16 не покрытых связей относятся к Avito V-Class: URL этого фильтра в
  исходной инструкции отсутствует; приблизительное назначение запрещено.

### Мониторинг

- Auto.ru parser адаптирован к текущим легковым и LCV путям.
- Owner no-VPN probe выявил актуальные индивидуальные URL новых автомобилей
  `/cars/new/group/.../<listing-id>-<slug>/`. Селектор добавлен, а сводные
  model-group карточки исключены из позиций. ID теперь берётся из начала
  последнего сегмента, а не из случайного цифрового хвоста slug.
- Avito parser имеет fixture-проверку известной структуры.
- Страницы 1–3, canonical marketplace ID, позиция на странице, абсолютная позиция
  и повторные эпизоды реализованы.
- CAPTCHA/429/неизвестная разметка/неполный проход дают technical, не absence.
- Первый валидный miss — uncertain; второй открывает confirmed episode.
- Scan и dealer discovery защищены от параллельного запуска.
- Scan отказывается работать при `local_vpn|unknown` до создания audit-записей.
- Серии непоказов разделены по сетевому профилю; только `cloud_no_vpn` открывает
  и закрывает production `AbsenceEpisode`.
- Скриншоты сохраняются в отдельный persistent volume с retention.

### Отчёт и люди

- HTML dashboard и каталог карточек проверены в desktop browser; каталог отдельно
  проверен в Chromium viewport 390×844 без горизонтального переполнения карточек.
- Каталог показывает 34 активных автомобиля, безопасные прямые ссылки площадок
  и честный placeholder до появления снимка успешного скана.
- История фильтруется по периоду, площадке, марке, модели и статусу; из неё можно
  перейти к автомобилю, объявлению и сохранённому PNG.
- В карточке автомобиля цена форматируется по тысячам, а найденные страницы
  агрегируются за всю историю в целом и отдельно по каждому поисковому фильтру.
- Видны KPI, operational status, импорты, фильтры, страницы/позиции, эпизоды,
  выходные, неполные ссылки, dealer-кандидаты и feedback.
- Текущие KPI исключают историю отключённых фильтров и недоверенные
  `local_vpn|unknown` наблюдения; полная audit history остаётся доступной.
- Feedback lifecycle и append-only audit пройдены end-to-end на stage.
- Добавлен отдельный экран «Ошибки объявлений»: фильтры очереди, карточка тикета,
  ответственный, допустимые переходы и журнал событий. Подтверждение исправления
  требует комментария с результатом проверки.
- Ручная правка ссылки валидируется и записывается в `ListingLinkEvent`.

### Эксплуатация

- Docker Compose поднимает app, PostgreSQL и ежедневный backup.
- Порты app/DB локально связаны только с `127.0.0.1`.
- Alembic `20260904_0004` применён к существующей stage-БД без потери реестра.
- Backup восстановлен в изолированную тестовую БД; контрольные количества совпали.
- Stage smoke проверяет readiness, импорт, status, dashboard, migration, порты,
  backup, restore и отсутствие traceback.
- Stage scheduler теперь fail-safe отключён; без `SCHEDULER_ENABLED=true` он не
  выполняет живой цикл при старте приложения.
- Отдельный `live_acceptance.sh` проверяет сетевой профиль, полноту каталога,
  новый ScanRun, technical-состояния и screenshot evidence; на текущем stage он
  ожидаемо заблокирован как `network_profile_local_vpn` до сетевого обращения.
- Suite: 72 теста; Ruff и `git diff --check` зелёные.
- Caddy cloud override подготовлен для HTTPS.

## Важная поправка к живым сетевым проверкам

Обычный Codex-сеанс на локальной машине работает через VPN. Поэтому:

- полученные Auto.ru кандидаты доказывают работу parser, но не production egress;
- Avito HTTP 429 ожидаем при VPN и не доказывает блокировку обычного cloud-IP;
- автоматический dealer discovery в локальном `.env` отключён;
- stage помечен `NETWORK_PROFILE=local_vpn`;
- production не стартует без явного `NETWORK_PROFILE=cloud_no_vpn`.

До добавления серверного gate один автозапуск старой stage-сборки успел создать
диагностические записи `local_vpn`: Auto.ru завершился частично, Avito получил
429. Эти append-only записи сохранены для аудита, явно помечены профилем и теперь
исключены из управленческой статистики. Они не считаются живой приёмкой.

Ранее сделанный вывод о недоступности Avito с обычного облачного IP отменён как
неподтверждённый. Это должно быть проверено на целевом сервере без VPN.

Отдельный Owner-run с временным профилем `local_no_vpn` выполнен 5 сентября:

- Auto.ru: 9 фильтров, 2 complete и 7 parser uncertainty на старой сборке;
  сохранённые PNG доказали, что страницы содержали новый тип карточек, после
  чего адаптер 0.4.1 исправлен и точечно распознал 28 V-Class и 2 Maextro offer;
- Avito: все 6 фильтров вернули HTTP 429 до разбора страницы. Это подтверждает
  блокировку текущего headless-клиента на домашнем no-VPN egress, но не отвечает
  за будущий cloud egress;
- второй запущенный Owner-run был прерван и не заменил завершённые записи.

## Открытые production-блокеры

1. Требуется утвердить отсутствующий в инструкции фильтр Avito V-Class; остальные
   16 уникальных сценариев внесены в канонический каталог.
2. Нет целевого VPS, домена и DNS, поэтому внешний HTTPS deploy не выполнен.
3. Не пройдена no-VPN проверка Auto.ru и Avito с production egress.
4. Не проведён 14-дневный пилот и ручная контрольная выборка.
5. Не настроен внешний канал уведомлений и список ответственных пользователей.
6. Требуется ротация обнаруженных ранее GitHub и DeepSeek credentials.
7. Требуется удалить публичное право записи на Drive-файл `SCRIPT.py`.

Пункты 1–3 требуют входных данных/инфраструктуры Owner и не могут быть корректно
«угаданы» кодом.

## Решение о готовности

Продуктовая кодовая база и локальный stage работоспособны; production не принят.
Следующий gate: целевой сервер без VPN + утверждённые фильтры, затем пилот.
