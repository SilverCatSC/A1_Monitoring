# Проверка локального обновления 0.7.0

Дата: 8 сентября 2026 года. Среда: текущий Mac, Python 3.12.14,
Google Chrome в отдельном тестовом процессе, Docker / PostgreSQL 16.

## Подтверждено

- `scripts/setup.sh` завершился с LOCAL_READY. Существующий .env сохранён.
- Пакет и конфигурация работающего контейнера: 0.7.0.
- Alembic: 20260908_0005 (head). Изменение схемы добавляет журнал реестра.
- `make check`: 114 passed; Ruff без ошибок.
- `git diff --check` и компиляция src/scripts без ошибок.
- `scripts/smoke_stage.sh`: LOCAL_READY, 8 разделов и CSS/JS отвечают HTTP 200.
- `make ui-check`: 8 разделов на 1440 и 390 px, плюс детализация автомобиля.
  Нет выхода документа за ширину окна и ошибок JavaScript.
  Широкие таблицы прокручиваются внутри своих блоков.
- Проверены сохранение периода/площадки при переходе из обзора в размещения,
  состояния обновления реестра и ошибки импорта. Тестовые состояния подставлялись
  только в изолированном браузере; в рабочую БД не добавлялись.
- Визуально просмотрены обзор, размещения и карточка автомобиля; исправлены
  мобильное переполнение старых блоков и обрезка карточечных изображений.

Скриншоты: `artifacts/ui_0_7/`. Это UI-доказательства, не живой обход площадок.

## Данные и резервирование

До обновления создана копия `/backups/a1_search_monitor_20260908T202325Z.dump`.
После миграции: `/backups/a1_search_monitor_20260908T202935Z.dump`.
Пути находятся в Docker-томе db_backups.

Тест восстановления второй копии в отдельную временную БД: RESTORE_OK,
106 автомобилей, 43 снимка импорта. Временная тестовая БД удалена после проверки;
рабочая база не заменялась. Этот тест сравнивает число автомобилей и импортов,
а не выполняет полную побайтовую проверку всех данных.

Отдельное чтение рабочей БД: 106 автомобилей, 648 наблюдений, 43 импорта,
0 новых событий ListingChangeEvent. Ноль ожидаем: живой импорт в рамках UI-работ
не выполнялся; журнал начнёт наполняться при последующих изменениях реестра.
Старые наблюдения и результаты не переписывались.

## Что не подтверждено этим обновлением

- Повторный живой обход Auto.ru/Avito не запускался. UI-тест блокирует внешнюю сеть.
- Отсутствие CAPTCHA и точность извлечения текущей выдачи не гарантируются.
- Причина поведения Auto.ru Zeekr и полнота короткой выдачи требуют
  отдельной контрольной проверки владельца без VPN.
- Недостающий утверждённый Avito V-Class URL не заменён приблизительным фильтром.
- Не установлен автозапуск при входе в macOS; --watch зависит от работающего компьютера.
- История импортированных цены/активности начинается с версии 0.7.0;
  дат публикации, показов, кликов и расходов площадок в источнике нет.
- В рабочем дереве сохранены изменения прежних этапов; commit/push не выполнялись.

Docker сообщил об отсутствии buildx-плагина, но успешно собрал образ обычным
сборщиком. Дополнительные плагины окружения не устанавливались.
